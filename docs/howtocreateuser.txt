sudo useradd username
sudo passwd username

log into that account by ssh:
$ whoami

# always allow developer ssh access
if [ $USER == 'pluto' ]
then
    source <foo.sh script> # this will run the remoteLoginClient
fi


# configure for project
sudo mkdir -p /var/lib/beagle-cam
sudo mkdir -p /var/log/beagle-cam

sudo touch /var/lib/beagle-cam/password.txt
sudo touch /var/lib/beagle-cam/mssecret.txt
sudo touch /var/lib/beagle-cam/settings.txt
sudo touch /var/log/beagle-cam/events.log

echo temppwd | sudo tee /var/lib/beagle-cam/password.txt
echo 111 | sudo tee /var/lib/beagle-cam/mssecret.txt
echo "0 1 60" | sudo tee /var/lib/beagle-cam/settings.txt


