#ifndef MENU_H_
#define MENU_H_

/**
 * handles the display menu on the bbg.
 * the main controls (on the joystick) are:
 * UP   => next
 * DOWN => previous
 * LEFT => go back
*/

void Menu_start(void);
void Menu_stop(void);


#endif